/driver
1. Migrate VGA driver code to the driver directory (currently in kernel/main.c) [ DONE ]
3. Implement better format specifiers in printd 
4. Implement itoa

mm.c
- move pic and pit code to setup_32.S:setup_32()

real_mode.s
- Change to real_mode.s
- Utilize #define's to get rid of hard coded values

mm.h
1. BITMAP_UNIT must be more generic such that number of bits in PAGES_PER_BYTE is not hardcoded as 8.

locking:
Brendan awesomeness: https://forum.osdev.org/viewtopic.php?t=28481
